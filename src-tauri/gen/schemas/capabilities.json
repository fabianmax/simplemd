{"default":{"identifier":"default","description":"Capability for every simplemd window. Labels are matched by glob: capabilities are scoped BY WINDOW LABEL, so a window opened as win-N gets no permissions at all unless it is listed here — not even event.listen, which leaves it silently inert.","local":true,"windows":["main","win-*"],"permissions":["core:default","core:window:allow-set-title","opener:default","dialog:default","opener:allow-open-path"]}}